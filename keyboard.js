// let b2Vec2 = Box2D.Common.Math.b2Vec2;

// let keys = [];

// function onKey(event) {
//     const touchName = event.key;

//     if (touchName === 'ArrowUp' || touchName === 'ArrowDown' || touchName === 'z' || touchName === 's' || touchName === 'r') {
//         if (keys.indexOf(touchName) == -1) {
//             keys.push(touchName);
//         }
//     }
// }

// function keyUp(event, bodyPOne, bodyPTwo) {
//     const touchName = event.key;

//     if (touchName === 'ArrowUp' || touchName === 'ArrowDown' || touchName === 'z' || touchName === 's') {
//         keys.splice(keys.indexOf(touchName), 1);
//         console.log(keys)
//     }
// }